const sliceStringFromBehindUntilSignal = (str) => str.slice(str.lastIndexOf('.'));

console.log(sliceStringFromBehindUntilSignal('hello.txt'));
console.log(sliceStringFromBehindUntilSignal('print.config.js'));

const process = require();


214241421